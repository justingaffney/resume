name: Generate resume artifacts

on:
  push:
    branches:
      - main

jobs:

  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install resume CLI and theme
        run: npm install resumed jsonresume-theme-elegant

      - name: Generate JSON
        uses: mikefarah/yq@master
        with:
          cmd: yq -p yaml -o json resume.yaml > resume.json

      - name: Validate resume.json
        run: npx resumed validate

      - name: Generate HTML
        run: npx resumed render resume.json -t jsonresume-theme-elegant -o resume.html

      - name: Generate PDF
        uses: fifsky/html-to-pdf-action@master
        with:
          htmlFile: resume.html
          outputFile: resume.pdf
          pdfOptions: '{"format": "A4", "margin": {"top": "10mm", "left": "10mm", "right": "10mm", "bottom": "10mm"}}'

      - name: Archive YAML
        uses: actions/upload-artifact@v4
        with:
          name: resume.yaml
          path: resume.yaml

      - name: Archive JSON
        uses: actions/upload-artifact@v4
        with:
          name: resume.json
          path: resume.json

      - name: Archive HTML
        uses: actions/upload-artifact@v4
        with:
          name: resume.html
          path: resume.html

      - name: Archive PDF
        uses: actions/upload-artifact@v4
        with:
          name: resume.pdf
          path: resume.pdf

      # TODO Update resume.json Gist
